<%# app/views/pages/index.html.erb %>
<%# — HEADER / NAV — %>
<%= render 'shared/header', @page.header_section %>

<%# render 'shared/header_navigation', @page.header_section %>
<%# — HERO BANNER — %>
<%= render 'shared/hero_banner', @page.hero_banner_section %>

<%# render 'shared/event_banner', @page.event_banner_section %>

<%= render 'shared/feature_section',  @page.feature_section %>
<%= render 'shared/bento_card_section',    @page.cards_section   %>
<%= render 'shared/dark_cta_section', @page.cta_section     %>
<%= render 'shared/pricing_section', @page.pricing_section %>
<%= render 'shared/newsletter_section', @page.newsletter_section %>
<%= render 'shared/stats_section', @page.stats_section %>
<%= render 'shared/testimonial_section', @page.testimonial_section %>
<%= render 'shared/blog_section', @page.blog_section %>
<%= render 'shared/contact_section', @page.contact_section %>
<%= render 'shared/leadership_section', @page.leadership_section %>
<%= render 'shared/workflow_section', @page.workflow_section %>
<%= render 'shared/trusted_by_section', @page.trusted_by_section %>
<%= render 'shared/settings_form', form: @page.settings_form %>
